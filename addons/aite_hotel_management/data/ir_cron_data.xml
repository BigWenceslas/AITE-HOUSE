<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_hotel_daily" model="ir.cron">
            <field name="name">Hôtel : tâches quotidiennes (no-show, ménage)</field>
            <field name="model_id" ref="model_aite_hotel_reservation"/>
            <field name="state">code</field>
            <field name="code">model._cron_daily_tasks()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
